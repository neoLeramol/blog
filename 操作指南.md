# Litos 博客操作指南（西电·个人博客）

> 项目位置：`C:\Users\Administrator\Desktop\blogs\Litos`
> 技术栈：Astro 6 + React 19 + TailwindCSS 4 + TypeScript（纯静态输出）
> 更新时间：2026-09-11

---

## 一、日常命令（最常用）

```bash
cd C:\Users\Administrator\Desktop\blogs\Litos

npx astro dev        # 开发预览（热更新，改代码立刻看到），http://localhost:4321
npx astro build      # 构建静态站点 → dist/
npx astro preview    # 预览构建结果（http://localhost:4321）
```

> ⚠️ 注意：用 `npx astro build` 而不是 `npm run build`（后者含 `astro check` 类型检查，本项目有 vite 类型冲突会报错，但不影响实际构建）

---

## 二、改站点信息（src/config.ts）

所有站点配置集中在这一个文件，改完保存即可看到效果：

### 1. 基础信息 SITE
```ts
export const SITE: Site = {
  title: 'Litos',                    // 网站标题
  description: '...',                // 网站描述（SEO）
  website: 'https://litos.vercel.app/',  // ⚠️ 改成你的域名
  lang: 'en',                        // 语言（可改 'zh-CN'）
  base: '/',                         // 部署路径（GitHub Pages 仓库名形式要改 '/Litos'）
  author: 'Dnzzk2',                  // ⚠️ 改成你的名字
  ogImage: '/og-image.webp',         // 社交分享图
  transition: false,                 // 页面过渡动画
  themeAnimation: true,              // 主题切换动画
}
```

### 2. 社交链接 SOCIAL_LINKS
```ts
export const SOCIAL_LINKS: SocialLink[] = [
  { name: 'github', url: 'https://github.com/你的用户名', icon: 'icon-[ri--github-fill]' },
  { name: 'twitter', url: 'https://x.com/你的用户名', icon: 'icon-[ri--twitter-x-fill]' },
]
```
> 图标查找：https://icon-sets.iconify.design/ （复制 icon 名，格式 `icon-[ri--xxx]`）

### 3. 导航栏 HEADER_LINKS / 页脚 FOOTER_LINKS
```ts
export const HEADER_LINKS: Link[] = [
  { name: 'Posts', url: '/posts' },
  { name: 'Projects', url: '/projects' },
  { name: 'Photos', url: '/photos' },
]
```

### 4. 文章列表 POSTS_CONFIG
```ts
{
  title: 'Posts',
  description: '文章页描述',
  author: '你的名字',
  homePageConfig: { size: 2, type: 'compact' },     // 首页展示 2 篇，紧凑布局
  postPageConfig: { size: 10, type: 'image', coverLayout: 'right' },  // 文章页布局
  readMoreText: 'Read more',        // "阅读更多"文案（可改中文）
}
```
**布局类型（type）**：`compact`（紧凑）/ `image`（封面图）
**封面布局（coverLayout）**：`right`（右侧）/ `top`（顶部）/ `split`（分栏）

### 5. 评论 COMMENT_CONFIG（Gitalk）
```ts
{
  enabled: true,                     // 关掉评论改 false
  system: 'gitalk',
  gitalk: {
    clientID: import.meta.env.PUBLIC_GITHUB_CLIENT_ID,      // 需在 .env 配
    clientSecret: import.meta.env.PUBLIC_GITHUB_CLIENT_SECRET,
    repo: 'gitalk-comment',          // ⚠️ 你的评论仓库名（需在 GitHub 建仓库）
    owner: '你的GitHub用户名',
    admin: ['你的GitHub用户名'],
    language: 'zh-CN',               // 中文化
  },
}
```
> 用 Gitalk 需要：①GitHub 建仓库存评论 ②创建 OAuth App 拿 clientID/Secret ③项目根目录建 `.env` 写 `PUBLIC_GITHUB_CLIENT_ID=xxx`、`PUBLIC_GITHUB_CLIENT_SECRET=xxx`

### 6. 访问统计 ANALYTICS_CONFIG
```ts
{ vercount: { enabled: true }, umami: { enabled: false, websiteId: '...', serverUrl: '...' } }
```

---

## 三、写文章（核心操作）

### 目录结构
```
src/content/posts/
└── 我的第一篇文章/           ← 目录名随意（英文/拼音/中文都行）
    ├── index.mdx            ← 文章正文
    └── assets/              ← 图片放这里
        └── cover.png        ← 封面图
```

### 文章头（frontmatter）字段表
```yaml
---
title: '我的第一篇文章'              # 必填：标题
description: '这是文章的摘要描述'     # 必填：描述（列表页显示）
pubDate: 2026-09-11                 # 必填：发布日期
author: '阿寒'                       # 作者（默认取配置里的）
cover: assets/cover.png             # 封面图路径（可选）
tags: ['学习', '集成电路']           # 标签（可选）
recommend: false                    # 是否推荐（可选）
pinned: false                       # 是否置顶（可选）
draft: false                        # true = 草稿不发布（可选）
updatedDate: 2026-09-12             # 更新日期（可选）
postType: 'metaOnly'                # 文章类型（可选）
---

正文用 Markdown 写 👇

## 二级标题

普通段落文字。

- 列表项
- 列表项

```python
# 代码块（自动高亮）
print("hello")
```

数学公式：$E = mc^2$
```

### 支持的语法（很强）
- **Markdown/MDX**：支持嵌入 React 组件
- **代码高亮**：Expressive Code（行号、折叠、标题）
- **数学公式**：KaTeX（`$...$` 行内，`$$...$$` 块级）
- **GitHub 卡片**：`<GitHubCard repo="owner/repo" />`（自定义插件）
- 图片自动优化（sharp）、阅读时长、标注框（rehype-callouts）

---

## 四、加项目展示

```
src/content/projects/
└── 我的项目/
    └── index.mdx
```

frontmatter：
```yaml
---
name: '项目名'
description: '项目描述'
githubUrl: 'https://github.com/你/项目'
website: 'https://项目网址'        # 没有就写 githubUrl
type: 'Web'                        # 项目类型标签
star: 10                           # star 数（手动填）
fork: 2
icon: assets/icon.png              # 图标（可选）
---
```

---

## 五、相册 & 技能展示

- **相册**：同 posts 结构，`src/content/posts/photos/` 下的图片素材；页面文案改 `PHOTOS_CONFIG`
- **技能展示**：`src/config.ts` 的 `SKILLSSHOWCASE_CONFIG`，`SKILLS_ENABLED: true/false` 开关 + 技能数组（图标 + 名称）

---

## 六、部署

### 方式 A：雨云服务器 + Nginx（推荐，国内访问最稳）
```bash
npx astro build                        # 生成 dist/
# 把 dist/ 拷到服务器（或用 Nginx 直接指向该目录）
```
Nginx 配置：
```nginx
server {
    listen 80;
    server_name 你的域名;
    root /path/to/Litos/dist;
    index index.html;
    location / { try_files $uri $uri/ /404.html; }
}
```

### 方式 B：GitHub Pages
1. 推到 GitHub 仓库
2. `src/config.ts` 改 `base: '/仓库名'`
3. GitHub Actions 自动构建部署（或用 `gh-pages` 分支放 dist/）
> ⚠️ 国内访问 GitHub Pages 不稳定

### 方式 C：Vercel/Netlify 一键
README 里有按钮，连 GitHub 仓库即可（国内访问一般）

---

## 七、常见坑（已踩过）

| 坑 | 解决 |
|---|---|
| **npm 装依赖后构建报 vite 类型错** | 项目锁定 vite 7.3.2，别用 npm 装出 vite 8；对齐版本：`npm i --save-exact vite@7.3.2 tailwindcss@4.2.2 @tailwindcss/vite@4.2.2` |
| `npm run build` 报 astro check 类型错 | 用 `npx astro build`（跳过类型检查，构建正常）|
| 外网访问不了 4321 | Windows 防火墙"公用网络"拦入站，加规则放行 |
| git clone 卡住 | 用 GitHub 加速镜像下载 zip：`https://gh-proxy.com/https://github.com/.../archive/refs/heads/main.zip` |
| 大文件下载截断 | 换镜像直连（代理对 >10MB 不稳）|

---

## 八、快速上手清单

- [ ] 改 `src/config.ts`：SITE（标题/作者/域名）+ SOCIAL_LINKS（你的 GitHub）
- [ ] 删掉示例文章：`src/content/posts/` 下 readme/start/enhance 等目录
- [ ] 写第一篇：新建 `src/content/posts/hello/index.mdx`
- [ ] 本地预览：`npx astro dev`
- [ ] 构建：`npx astro build`
- [ ] 部署：拷 dist/ 到雨云 + Nginx
